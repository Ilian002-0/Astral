// This static data is no longer used. The app now fetches live daily data
// for benchmark comparisons from a user-configured Google Sheet URL.
export const spyData: { date: string; price: number }[] = [];
